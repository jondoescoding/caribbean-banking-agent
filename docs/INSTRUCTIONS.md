# Documentation brief

Maintain a compact, source-aware repository wiki for a three-day Future Caribbean buildathon project. Prioritize the five-minute demo path, the read-only safety boundary, the ChatGPT MCP tool contract, Browserbase operating knowledge, JMMB data semantics, and commands another agent needs to resume work.

Every article needs a stable `id`, a short title, a lifecycle `status`, and `related` IDs in YAML frontmatter. Separate confirmed implementation facts from proposed later versions, cite primary sources for external behavior, and never copy credentials or personal banking data into documentation.
