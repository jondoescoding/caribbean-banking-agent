{
  "name": "caribbean-banking-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Caribbean banking API backed by Browserbase",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace @caribbean-banking-agent/backend",
    "check": "npm run check --workspace @caribbean-banking-agent/backend",
    "dev": "npm run dev --workspace @caribbean-banking-agent/backend",
    "start": "npm run start --workspace @caribbean-banking-agent/backend",
    "test": "npm run test --workspace @caribbean-banking-agent/backend"
  },
  "engines": {
    "node": ">=22"
  }
}
